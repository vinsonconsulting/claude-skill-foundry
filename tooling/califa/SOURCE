Vendored from: vinsonconsulting/califa-cards
Commit:        89d52f6a73f66151b6571f400e907c2a634cde7b  (v0.7.0)
Tag:           v0.7.0
Vendored on:   2026-06-22
License:       Apache-2.0 (see ./LICENSE)

Pin note: 89d52f6 is the v0.7.0 release (merge of PR #10, "Skill-card v0.7.0:
eval rate-limit resilience"). It supersedes the prior pin f975d4b (v0.6.2 plus
two eval-path fixes on fix/functional-eval-copy-references) — those eval-path
fixes are folded into v0.7.0's history. v0.7.0 adds the eval rate-limit
resilience layer (new `skillcard/harness/reliability.py`: TokenBucket pacer +
call_with_retry backoff/jitter + ReliabilityStats), the eval/optimize CLI knobs
`--rate-limit` / `--max-retries` / `--task-timeout` / `--backoff-base` /
`--backoff-cap`, and a terminal-failure-only integrity guard. All additive on
the eval path; the card build/render/hash path is unchanged.

This is a pinned copy of Califa's `schema/` + `skillcard/` packages (including
the `skillcard.harness` subpackage, dormant here — `make check` never runs
`skillcard eval`), plus the Jinja template and pyproject, so the public
cabinet's `make check` can validate and gate skill cards without a private-repo
CI secret.

Install:  python3 -m pip install -e "tooling/califa[scan]"

v2 exit: replace this vendored copy with a pinned dependency once Califa
publishes a tagged release / package.
